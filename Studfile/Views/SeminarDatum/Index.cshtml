@model IEnumerable<Studfile.Models.SeminarDatum>

@{
    ViewBag.Title = "Index";
}

<h2>Ponuđeni termini izlaganja</h2>

<p>
    @Html.ActionLink("Ponudite novi termin", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TerminIzlaganja)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TerminIzlaganja)
        </td>
        
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id = item.Id }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.Id }) |*@
            @Html.ActionLink("Obriši", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
